{
    "rules": {
        "telemetria": {
            "$mac": {
                ".read": "auth != null",
                ".write": "auth != null && (auth.token.role == 'ADMIN' || auth.token.role == 'DISPOSITIVO')"
            }
        }
    }
}